"""Отрисовка сцены ProtoCAD: общий вьюпорт для приложения и для ПРОТО.

Пакет намеренно **не зависит от ядра геометрии**: на вход приходят готовые
буферы вершин, нормалей, идентификаторов и рёбер. Поэтому его одинаково
используют CAD-приложение и просмотровый модуль внутри закрытого ПРОТО, а
лицензионные контуры остаются раздельными.

Зависимости: numpy, PySide6, PyOpenGL. Ни OCCT, ни решателя, ни FreeCAD.

Устройство сцены — причина, по которой это работает быстро: вся геометрия
лежит в ОДНОМ буфере GPU и рисуется двумя вызовами (заливка и рёбра). Обхода
графа на кадр нет, тесселяция считается один раз при загрузке.
"""

from .viewport import SceneBuffers, Viewport

__all__ = ["SceneBuffers", "Viewport"]
