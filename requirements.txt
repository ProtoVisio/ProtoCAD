# Зависимости ProtoCAD. FreeCAD среди них нет — приложение самостоятельное.
# Проверено Spike 4: геометрия, проекции, обмен и параметрика работают
# напрямую на OCCT.

# --- ядро геометрии ---
cadquery-ocp>=7.9          # OCCT 7.9 через привязки OCP, Apache-2.0
numpy>=1.26                # буферы вершин и матрицы, BSD-3

# --- интерфейс ---
PySide6>=6.6               # окна и контекст OpenGL, LGPL-3
PyOpenGL>=3.1              # вызовы GL, BSD

# --- решатель ограничений: ВЫБОР ЛИЦЕНЗИИ ---
# Ставится один из двух. Реестр в protocad/sketch/solver.py показывает,
# какой доступен и что каждый означает для лицензии приложения.
#
#   planegcs — LGPL-2.1+, решатель из FreeCAD. Приложение остаётся свободным
#              в выборе лицензии. Требует Python >= 3.12.
#   py-slvs  — GPL-3, SolveSpace. Связывает GPL-3 всё приложение.
#
# По умолчанию берём planegcs как менее обязывающий.
planegcs>=0.8; python_version >= "3.12"
py-slvs>=1.0
